"use client";
import { motion } from "framer-motion";
import type { DemoCitation } from "./demo-data";
export function DemoCitations({ citations, onSelect }: { citations: DemoCitation[]; onSelect: (source: string) => void }) { return <div className="flex flex-wrap gap-2">{citations.map((citation) => <motion.button initial={{ opacity: 0, y: 5 }} animate={{ opacity: 1, y: 0 }} type="button" key={citation.source + citation.detail} onClick={() => onSelect(citation.source)} className="group relative rounded-full border border-violet-300/20 bg-violet-400/[.08] px-3 py-1.5 text-xs text-violet-200 hover:bg-violet-400/[.16]"><span>{citation.source} · {citation.detail}</span><span className="pointer-events-none absolute bottom-full left-0 z-10 mb-2 hidden w-64 rounded-xl border border-white/[.1] bg-zinc-950 p-3 text-left shadow-2xl group-hover:block"><span className="block font-medium text-white">{citation.source}</span><span className="mt-1 block text-[11px] text-zinc-500">{citation.detail}</span><span className="mt-2 block leading-5 text-zinc-400">{citation.preview}</span><span className="mt-2 block text-[11px] font-medium text-violet-300">Open Source</span></span></motion.button>)}</div>; }
